// This part stays on the front-end. The part that does not is the Quiz, that only contains information, therefore can be stored in the backend
// Later, you can look up how to get only what you're going to use in a page, like a dynamic import from the backend, or an API call to get the theme's jsx and images instead of having all of them all the time
const themes = {};
